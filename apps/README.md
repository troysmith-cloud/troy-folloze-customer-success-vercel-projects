# Apps

Create each deployable Vercel app in its own folder here.

Example:

```text
apps/
  customer-portal/
    package.json
    app/
    public/
```

In Vercel, set the project Root Directory to the app folder, for example `apps/customer-portal`.
